rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    match /audio/{allPaths=**} {
      allow read, write: if request.auth != null || true; // dev: restrict later
    }
    match /peaks/{allPaths=**} {
      allow read, write: if request.auth != null || true;
    }
  }
}

